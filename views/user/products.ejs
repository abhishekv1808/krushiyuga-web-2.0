<% // SEO data for products page const
    pageTitle="Premium Livestock & Agricultural Products - Goat Breeds, Feed & Equipment | Krushiyuga" ; const
    pageDescription="Discover Krushiyuga's premium livestock products including Osmanabadi goats, quality feed, farming equipment, and agricultural supplies for modern farming in Karnataka."
    ; const
    pageKeywords="goat breeds Karnataka, Osmanabadi goats for sale, livestock feed, farming equipment, agricultural products, goat farming supplies, dairy products"
    ; const currentUrl="https://krushiyuga.com/products" ; const ogImage="https://krushiyuga.com/favicon.svg" ; %>
    <%- include('../partials/head.ejs') %>


        <body>
            <%- include('../partials/navbar.ejs') %>

                <main>


                    <section class="hero-bg min-h-screen sm:min-h-150 flex items-center relative overflow-hidden">
                        <div class="container mx-auto max-w-screen-xl px-4 sm:px-6 py-16 sm:py-20 relative z-10">
                            <div class="text-center">
                                <div class="inline-block mb-4 sm:mb-6">
                                    <span
                                        class="bg-emerald-100 text-emerald-800 px-4 sm:px-6 py-2 sm:py-3 rounded-full text-xs sm:text-sm lg:text-base font-medium">
                                        🐄 Premium Livestock
                                    </span>
                                </div>

                                <h1
                                    class="text-3xl sm:text-4xl md:text-5xl lg:text-6xl font-bold text-white leading-tight mb-4 sm:mb-6 fade-in">
                                    Our <span class="text-emerald-400">Premium</span>
                                    <br>Livestock
                                </h1>

                                <p class="text-sm sm:text-base md:text-lg lg:text-xl xl:text-2xl text-gray-200 max-w-3xl mx-auto mb-6 sm:mb-8 fade-in px-4"
                                    style="animation-delay: 0.2s;">
                                    Explore our exceptional collection of Osmanabadi goats, sheep, and poultry through
                                    our
                                    comprehensive
                                    gallery
                                </p>

                                <div class="flex flex-col sm:flex-row justify-center gap-3 sm:gap-4 fade-in px-4"
                                    style="animation-delay: 0.4s;">
                                    <button
                                        class="cursor-pointer bg-emerald-600 text-white px-6 sm:px-8 py-3 sm:py-4 rounded-xl sm:rounded-2xl font-semibold text-sm sm:text-base lg:text-lg hover:bg-emerald-700 transition-colors pulse-glow">
                                        <i class="fas fa-images mr-2"></i>View Gallery
                                    </button>
                                    <a href="/contact-us"
                                        class="cursor-pointer border-2 border-white text-white px-6 sm:px-8 py-3 sm:py-4 rounded-xl sm:rounded-2xl font-semibold text-sm sm:text-base lg:text-lg hover:bg-white hover:text-emerald-600 transition-colors">
                                        <i class="fas fa-phone mr-2"></i>Contact Us
                                    </a>
                                </div>
                            </div>
                        </div>

                        <!-- Floating Elements - Hidden on mobile -->
                        <div class="hidden lg:block absolute top-20 right-20 text-white opacity-20 floating-animation">
                            <i class="fas fa-leaf text-6xl"></i>
                        </div>
                        <div class="hidden lg:block absolute bottom-20 left-20 text-white opacity-20 floating-animation"
                            style="animation-delay: 1s;">
                            <i class="fas fa-mountain text-5xl"></i>
                        </div>
                    </section>

                    <!-- Gallery Section -->
                    <section class="py-20 bg-white">
                        <div class="container mx-auto max-w-screen-xl px-6">
                            <!-- Section Header -->
                            <div class="text-center mb-16">
                                <h2 class="text-5xl font-bold text-gray-900 mb-6">
                                    Premium <span class="gradient-text">Livestock</span> Collection
                                </h2>
                                <p class="text-lg text-gray-600 max-w-2xl mx-auto">
                                    Discover our carefully curated selection of high-quality livestock including
                                    Osmanabadi
                                    goats, sheep,
                                    and poultry
                                </p>
                            </div>

                            <!-- Category Filters -->
                            <div class="flex flex-wrap justify-center gap-4 mb-12">
                                <button
                                    class="cursor-pointer category-btn active px-8 py-3 rounded-full font-semibold text-gray-700 bg-gray-100 hover:bg-emerald-600 hover:text-white">
                                    All Livestock
                                </button>
                                <button
                                    class="cursor-pointer category-btn px-8 py-3 rounded-full font-semibold text-gray-700 bg-gray-100 hover:bg-emerald-600 hover:text-white">
                                    🐐 Osmanabadi Goats
                                </button>
                                <button
                                    class="cursor-pointer category-btn px-8 py-3 rounded-full font-semibold text-gray-700 bg-gray-100 hover:bg-emerald-600 hover:text-white">
                                    🐑 Sheep
                                </button>
                                <button
                                    class="cursor-pointer category-btn px-8 py-3 rounded-full font-semibold text-gray-700 bg-gray-100 hover:bg-emerald-600 hover:text-white">
                                    🐔 Poultry
                                </button>
                            </div>

                            <!-- Products Count (optional) -->
                            <% if (products && products.length> 0) { %>
                                <p class="text-center text-gray-600 mb-8">Showing <%= products.length %> products</p>
                                <% } %>

                                    <!-- Gallery Grid -->
                                    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
                                        <% if (products && products.length> 0) { %>
                                            <% products.forEach(product=> { %>
                                                <div class="gallery-card bg-white rounded-xl overflow-hidden shadow-md relative group transform transition-all duration-300 hover:-translate-y-1 hover:shadow-lg border border-gray-100"
                                                    data-category="<%= product.category ? product.category.toLowerCase().replace(/\s+/g, '-') : 'other' %>">
                                                    <!-- Image Section with Badge -->
                                                    <div class="relative overflow-hidden">
                                                        <img src="<%= product.imageUrl || 'https://images.unsplash.com/photo-1516467508483-a7212febe31a?ixlib=rb-4.0.3&auto=format&fit=crop&w=800&q=80' %>"
                                                            alt="<%= product.name %>"
                                                            class="w-full h-48 object-cover transition-transform duration-500 group-hover:scale-110">
                                                        <!-- Quick View Button -->
                                                        <button onclick="showItemDetails(
                                                        `<%= encodeURIComponent(product.name) %>`,
                                                        `₹<%= encodeURIComponent(product.price) %>`,
                                                        `<%= encodeURIComponent(product.description) %>`,
                                                        `<%= encodeURIComponent(product.imageUrl || 'https://images.unsplash.com/photo-1516467508483-a7212febe31a?ixlib=rb-4.0.3&auto=format&fit=crop&w=800&q=80') %>`
                                                    )" class="absolute bottom-4 right-4 bg-white/90 backdrop-blur-sm text-emerald-600 p-2 rounded-full shadow-sm opacity-0 group-hover:opacity-100 transition-opacity duration-300 hover:bg-white">
                                                            <i class="fas fa-eye"></i>
                                                        </button>
                                                        <!-- Category Badge -->
                                                        <div class="absolute top-4 left-4">
                                                            <span
                                                                class="bg-white/90 backdrop-blur-sm text-emerald-700 px-3 py-1 rounded-full text-xs font-medium shadow-sm">
                                                                <%= product.category %>
                                                            </span>
                                                        </div>
                                                    </div>

                                                    <!-- Content Section -->
                                                    <div class="p-4">
                                                        <!-- Title and Price -->
                                                        <div class="mb-2">
                                                            <div class="flex justify-between items-start gap-2">
                                                                <h3
                                                                    class="text-base font-semibold text-gray-900 group-hover:text-emerald-600 transition-colors line-clamp-1">
                                                                    <%= product.name %>
                                                                </h3>
                                                                <span
                                                                    class="text-lg font-bold text-emerald-600 whitespace-nowrap">₹
                                                                    <%= product.price %>
                                                                </span>
                                                            </div>
                                                        </div>

                                                        <!-- Product Details -->
                                                        <div class="space-y-2">
                                                            <!-- Key Features Grid -->
                                                            <div class="grid grid-cols-2 gap-2 text-sm">
                                                                <div class="flex items-center space-x-1 text-gray-600">
                                                                    <i
                                                                        class="fas fa-weight-hanging text-emerald-500 text-xs"></i>
                                                                    <span class="truncate">
                                                                        <%= product.weight %>
                                                                    </span>
                                                                </div>
                                                                <div class="flex items-center space-x-1 text-gray-600">
                                                                    <i
                                                                        class="fas fa-calendar text-emerald-500 text-xs"></i>
                                                                    <span class="truncate">
                                                                        <%= product.age %>
                                                                    </span>
                                                                </div>
                                                                <div class="flex items-center space-x-1 text-gray-600">
                                                                    <i class="fas fa-box text-emerald-500 text-xs"></i>
                                                                    <span class="truncate">Stock: <%= product.quantity
                                                                            %>
                                                                    </span>
                                                                </div>
                                                                <div class="flex items-center space-x-1 text-gray-600">
                                                                    <i class="fas fa-tag text-emerald-500 text-xs"></i>
                                                                    <span class="truncate">ID: #<%=
                                                                            product._id.toString().slice(-4) %></span>
                                                                </div>
                                                            </div>

                                                            <!-- Description -->
                                                            <p
                                                                class="text-sm text-gray-500 line-clamp-2 min-h-[2.5rem]">
                                                                <%= product.description %>
                                                            </p>
                                                        </div>

                                                        <!-- Action Buttons -->
                                                        <div class="grid grid-cols-2 gap-2 mt-3">
                                                            <button
                                                                onclick="contactForItem('<%= encodeURIComponent(product.name) %>')"
                                                                class="w-full bg-emerald-600 text-white px-3 py-2 rounded-lg text-sm font-medium hover:bg-emerald-700 transition-colors flex items-center justify-center space-x-1">
                                                                <i class="fas fa-phone text-xs"></i>
                                                                <span>Contact</span>
                                                            </button>
                                                            <button onclick="showItemDetails(
                                                            `<%= encodeURIComponent(product.name) %>`,
                                                            `₹<%= encodeURIComponent(product.price) %>`,
                                                            `<%= encodeURIComponent(product.description) %>`,
                                                            `<%= encodeURIComponent(product.imageUrl || 'https://images.unsplash.com/photo-1516467508483-a7212febe31a?ixlib=rb-4.0.3&auto=format&fit=crop&w=800&q=80') %>`
                                                        )" class="w-full border border-emerald-600 text-emerald-600 px-3 py-2 rounded-lg text-sm font-medium hover:bg-emerald-50 transition-colors flex items-center justify-center space-x-1">
                                                                <i class="fas fa-info-circle text-xs"></i>
                                                                <span>Details</span>
                                                            </button>
                                                        </div>
                                                    </div>
                                                </div>
                                                <% }); %>
                                                    <% } else { %>
                                                        <div class="col-span-full text-center py-12">
                                                            <i class="fas fa-box-open text-6xl text-gray-300 mb-4"></i>
                                                            <h3 class="text-xl font-medium text-gray-600">No products
                                                                available at the moment</h3>
                                                            <p class="text-gray-500 mt-2">Please check back later or
                                                                contact
                                                                us for more information</p>
                                                        </div>
                                                        <% } %>
                                    </div>
                        </div>
                    </section>

                    <!-- CTA Section -->
                    <section class="py-20 bg-gradient-to-r from-emerald-600 to-emerald-800">
                        <div class="container mx-auto max-w-screen-xl px-6 text-center">
                            <h2 class="text-4xl font-bold text-white mb-4">Ready to Start Your Livestock Journey?</h2>
                            <p class="text-xl text-emerald-100 mb-8 max-w-2xl mx-auto">
                                Contact us today to discuss your livestock needs and explore our premium collection
                            </p>
                            <div class="flex flex-col sm:flex-row gap-4 justify-center">
                                <button onclick="window.location.href='tel:+919900077949'"
                                    class="bg-white text-emerald-600 px-8 py-4 rounded-2xl font-semibold text-lg hover:bg-gray-100 transition-colors">
                                    <i class="fas fa-phone mr-2"></i>Call Now
                                </button>
                                <a href="/contact-us"
                                    class="border-2 border-white text-white px-8 py-4 rounded-2xl font-semibold text-lg hover:bg-white hover:text-emerald-600 transition-colors text-center">
                                    <i class="fas fa-envelope mr-2"></i>Contact Us
                                </a>
                            </div>
                        </div>
                    </section>
                </main>

                <%- include('../partials/footer.ejs') %>


                    <script>
                        // Category filter functionality with enhanced features
                        const categoryBtns = document.querySelectorAll('.category-btn');
                        const galleryCards = document.querySelectorAll('.gallery-card');

                        // Define category mappings
                        const categoryMap = {
                            'All Livestock': 'all',
                            '🐐 Osmanabadi Goats': 'osmanabadi-goat',
                            '🐑 Sheep': 'sheep',
                            '🐔 Poultry': 'poultry'
                        };

                        // Categories are now assigned via data-category in the HTML
                        function assignCategories() {
                            // No need to manually assign categories as they come from the database
                            // Categories assigned from database
                        }

                        // Filter cards with smooth animation
                        function filterCards(category) {
                            galleryCards.forEach((card, index) => {
                                const cardCategory = card.dataset.category;
                                const shouldShow = category === 'all' || cardCategory === category || cardCategory === 'general';

                                if (shouldShow) {
                                    card.style.display = 'block';
                                    setTimeout(() => {
                                        card.style.opacity = '1';
                                        card.style.transform = 'translateY(0)';
                                    }, index * 50);
                                } else {
                                    card.style.opacity = '0';
                                    card.style.transform = 'translateY(20px)';
                                    setTimeout(() => {
                                        card.style.display = 'none';
                                    }, 300);
                                }
                            });
                        }

                        // Add event listeners to category buttons
                        categoryBtns.forEach(btn => {
                            btn.addEventListener('click', () => {
                                // Remove active class from all buttons
                                categoryBtns.forEach(b => {
                                    b.classList.remove('active');
                                    b.classList.remove('bg-emerald-600', 'text-white');
                                    b.classList.add('bg-gray-100', 'text-gray-700');
                                });

                                // Add active class to clicked button
                                btn.classList.add('active');
                                btn.classList.remove('bg-gray-100', 'text-gray-700');
                                btn.classList.add('bg-emerald-600', 'text-white');

                                // Get category and filter
                                const category = categoryMap[btn.textContent.trim()];
                                filterCards(category);

                                // Update URL hash for bookmarking
                                window.location.hash = category;
                            });
                        });

                        // Gallery card hover effects and interactions
                        galleryCards.forEach(card => {
                            const img = card.querySelector('img');
                            const overlay = card.querySelector('.overlay-content');
                            const viewMoreBtn = card.querySelector('button');

                            // Enhanced hover effects
                            card.addEventListener('mouseenter', () => {
                                overlay.style.opacity = '1';
                                overlay.style.background = 'rgba(0, 0, 0, 0.6)';
                                card.style.transform = 'translateY(-5px)';
                                card.style.boxShadow = '0 20px 40px rgba(0, 0, 0, 0.15)';
                            });

                            card.addEventListener('mouseleave', () => {
                                overlay.style.opacity = '0';
                                overlay.style.background = 'transparent';
                                card.style.transform = 'translateY(0)';
                                card.style.boxShadow = '0 10px 20px rgba(0, 0, 0, 0.1)';
                            });

                            // View More button functionality
                            viewMoreBtn.addEventListener('click', (e) => {
                                e.preventDefault();
                                const title = card.querySelector('h3').textContent;
                                const price = card.querySelector('.text-emerald-600').textContent;
                                const description = card.querySelector('.text-gray-600').textContent;

                                // Create modal or alert with details
                                showItemDetails(title, price, description, img.src);
                            });
                        });

                        // Show item details function
                        function showItemDetails(title, price, description, imgSrc) {
                            // Decode the URI-encoded values
                            const decodedTitle = decodeURIComponent(title);
                            const decodedPrice = decodeURIComponent(price);
                            const decodedDesc = decodeURIComponent(description);
                            const decodedImgSrc = decodeURIComponent(imgSrc);

                            // Create modal overlay
                            const modal = document.createElement('div');
                            modal.className = 'fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50 p-4';
                            modal.innerHTML = `
                            <div class="bg-white rounded-3xl p-8 max-w-2xl w-full relative transform transition-all">
                                <button class="absolute top-6 right-6 text-gray-500 hover:text-gray-700 focus:outline-none" onclick="this.parentElement.parentElement.remove()">
                                    <i class="fas fa-times text-xl"></i>
                                </button>

                                <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
                                    <!-- Image Section -->
                                    <div class="space-y-4">
                                        <div class="aspect-w-4 aspect-h-3 rounded-2xl overflow-hidden">
                                            <img src="${decodedImgSrc}" alt="${decodedTitle}" class="w-full h-full object-cover">
                                        </div>
                                        <div class="bg-emerald-50 rounded-xl p-4">
                                            <div class="font-semibold text-emerald-800 mb-2">Contact Information:</div>
                                            <div class="space-y-2 text-emerald-600">
                                                <div class="flex items-center space-x-2">
                                                    <i class="fas fa-phone"></i>
                                                    <span>+91 9900077949</span>
                                                </div>
                                                <div class="flex items-center space-x-2">
                                                    <i class="fas fa-envelope"></i>
                                                    <span>info@krushiyuga.com</span>
                                                </div>
                                                <div class="flex items-center space-x-2">
                                                    <i class="fas fa-map-marker-alt"></i>
                                                    <span>Osmanabad, Karnataka</span>
                                                </div>
                                            </div>
                                        </div>
                                    </div>

                                    <!-- Content Section -->
                                    <div class="space-y-6">
                                        <div>
                                            <h3 class="text-2xl font-bold text-gray-900 mb-2">${decodedTitle}</h3>
                                            <div class="flex items-center space-x-2 text-emerald-600 text-2xl font-bold">
                                                ${decodedPrice}
                                            </div>
                                        </div>

                                        <div class="space-y-4">
                                            <div class="prose text-gray-600">
                                                ${decodedDesc}
                                            </div>
                                        </div>

                                        <div class="border-t border-gray-200 pt-6">
                                            <div class="text-gray-700 font-semibold mb-4">Why Choose Krushiyuga?</div>
                                            <div class="grid grid-cols-1 gap-4">
                                                <div class="flex items-center space-x-3 text-gray-600">
                                                    <i class="fas fa-check-circle text-emerald-500"></i>
                                                    <span>Premium Quality Livestock</span>
                                                </div>
                                                <div class="flex items-center space-x-3 text-gray-600">
                                                    <i class="fas fa-check-circle text-emerald-500"></i>
                                                    <span>Expert Guidance & Support</span>
                                                </div>
                                                <div class="flex items-center space-x-3 text-gray-600">
                                                    <i class="fas fa-check-circle text-emerald-500"></i>
                                                    <span>Transparent Pricing</span>
                                                </div>
                                                <div class="flex items-center space-x-3 text-gray-600">
                                                    <i class="fas fa-check-circle text-emerald-500"></i>
                                                    <span>After-sale Support</span>
                                                </div>
                                            </div>
                                        </div>

                                        <div class="flex gap-4 pt-4">
                                            <button onclick="window.location.href='tel:+919900077949'" class="flex-1 bg-emerald-600 text-white px-6 py-3 rounded-xl font-semibold hover:bg-emerald-700 transition-colors flex items-center justify-center space-x-2">
                                                <i class="fas fa-phone"></i>
                                                <span>Call Now</span>
                                            </button>
                                            <button onclick="window.location.href='/contact-us'" class="flex-1 border-2 border-emerald-600 text-emerald-600 px-6 py-3 rounded-xl font-semibold hover:bg-emerald-50 transition-colors flex items-center justify-center space-x-2">
                                                <i class="fas fa-envelope"></i>
                                                <span>Enquire</span>
                                            </button>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        `;
                            document.body.appendChild(modal);

                            // Close modal when clicking outside
                            modal.addEventListener('click', (e) => {
                                if (e.target === modal) {
                                    modal.remove();
                                }
                            });

                            // Close modal on escape key
                            document.addEventListener('keydown', (e) => {
                                if (e.key === 'Escape') {
                                    modal.remove();
                                }
                            });
                        }

                        // Contact for item function
                        function contactForItem(title) {
                            const decodedTitle = decodeURIComponent(title);
                            window.location.href = `/contact-us?product=${decodedTitle}`;
                        }
                    </script>


        </body>

        </html>
